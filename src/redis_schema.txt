handles[str]    uid

user:last       uid
user:{uid}
    handle      str
    phash       str
    joined_at   epoch
    referred_by uid

token:{uid}     str

referral:{str}  uid

referral_cooldown:{uid} 1
